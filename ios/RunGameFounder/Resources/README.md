# Local founder resources

Run `python3 tools/r02_prepare_ios.py` from the repository root before generating
or building the Xcode project. The command creates the ignored `Local/` folder
with the active mission configuration, M4A master, and manifest.
